# Copy this file to .env and fill in your CoinGecko Demo API key.
# Get one for free: https://www.coingecko.com/en/api/pricing
#
# Without a key the app uses the public CoinGecko tier (low rate limit).
# With a Demo key it raises the limit and the build embeds it as a header.

COINGECKO_API_KEY=
